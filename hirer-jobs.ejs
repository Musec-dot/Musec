<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Job Postings</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        /* Floating Navbar Styles */
        .navbar-container {
            position: sticky;
            top: 0;
            z-index: 100;
            display: flex;
            justify-content: center;
            padding: 1.5rem 1rem 1rem 1rem;
            background-color: rgba(5, 5, 5, 0.95);
            backdrop-filter: blur(8px);
            -webkit-backdrop-filter: blur(8px);
        }
        .navbar {
            background-color: #121212;
            border-radius: 9999px;
            padding: 0.75rem 1.5rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
            width: 100%;
            max-width: 64rem;
            border: 1px solid rgba(255, 255, 255, 0.05);
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.3), 0 4px 6px -2px rgba(0, 0, 0, 0.1);
        }
        .search-container {
            position: relative;
        }
        .search-icon {
            position: absolute;
            left: 0.75rem;
            top: 50%;
            transform: translateY(-50%);
            color: #9ca3af;
            pointer-events: none;
            width: 18px;
            height: 18px;
        }
        .search-input {
            background-color: #1e1e1e;
            color: #d1d5db;
            font-size: 0.875rem;
            border-radius: 9999px;
            padding: 0.625rem 1rem 0.625rem 2.5rem;
            width: 16rem;
            border: none;
            outline: none;
            transition: all 0.2s;
        }
        .search-input:focus {
            box-shadow: 0 0 0 1px #4b5563;
            background-color: #252525;
        }
        .nav-links {
            display: flex;
            align-items: center;
            gap: 2rem;
        }
        .nav-item {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 0.25rem;
            cursor: pointer;
            color: #9ca3af;
            transition: color 0.2s;
            text-decoration: none;
        }
        .nav-item:hover {
            color: #e5e7eb;
        }
        .nav-item.active {
            color: #ffdec1;
        }
        .nav-item .icon-wrapper {
            transition: transform 0.2s;
        }
        .nav-item:hover .icon-wrapper {
            transform: scale(1.1);
        }
        .nav-label {
            font-size: 10px;
            font-weight: 500;
            letter-spacing: 0.025em;
        }
        .user-avatar {
            width: 2.5rem;
            height: 2.5rem;
            border-radius: 50%;
            background: linear-gradient(135deg, #9ca3af, #4b5563);
            border: 2px solid #121212;
            cursor: pointer;
            transition: opacity 0.2s;
        }
        .user-avatar:hover {
            opacity: 0.8;
        }
    </style>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: #000;
            color: #fff;
            min-height: 100vh;
            padding-top: 0;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
        }

        h1 {
            font-size: 2.5rem;
            background: linear-gradient(135deg, #fff 0%, #ff6b35 60%, #ff8c42 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .post-job-btn {
            background-color: #fff;
            color: #000;
            border: none;
            padding: 0.875rem 2rem;
            font-size: 1rem;
            font-weight: 600;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
        }

        .post-job-btn:hover {
            background-color: #f0f0f0;
            transform: translateY(-1px);
        }

        .jobs-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 1.5rem;
        }

        .job-card {
            background-color: #111;
            border-radius: 16px;
            padding: 1.5rem;
            border: 1px solid #333;
            transition: all 0.3s ease;
        }

        .job-card:hover {
            border-color: #ff6b35;
            transform: translateY(-2px);
        }

        .job-card h3 {
            font-size: 1.25rem;
            margin-bottom: 0.5rem;
            color: #fff;
        }

        .job-card .meta {
            color: #999;
            font-size: 0.9rem;
            margin-bottom: 1rem;
        }

        .job-card .description {
            color: #ccc;
            font-size: 0.9rem;
            margin-bottom: 1rem;
            line-height: 1.5;
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .job-card .payment {
            color: #ff6b35;
            font-weight: 600;
            margin-bottom: 1rem;
        }

        .job-actions {
            display: flex;
            gap: 0.75rem;
        }

        .view-applications-btn {
            background-color: #ff6b35;
            color: #fff;
            border: none;
            padding: 0.625rem 1.25rem;
            font-size: 0.9rem;
            font-weight: 600;
            border-radius: 6px;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
            flex: 1;
            text-align: center;
        }

        .view-applications-btn:hover {
            background-color: #ff8c42;
        }

        .status-badge {
            display: inline-block;
            padding: 0.25rem 0.75rem;
            border-radius: 12px;
            font-size: 0.75rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
        }

        .status-open {
            background-color: rgba(76, 175, 80, 0.2);
            color: #4caf50;
        }

        .status-closed {
            background-color: rgba(244, 67, 54, 0.2);
            color: #f44336;
        }

        .empty-state {
            text-align: center;
            padding: 4rem 2rem;
            color: #666;
        }

        .empty-state h2 {
            font-size: 1.5rem;
            margin-bottom: 1rem;
            color: #999;
        }
    </style>
</head>
<body>
    <!-- Floating Navbar -->
    <div class="navbar-container">
        <nav class="navbar">
            <div class="search-container">
                <i data-lucide="search" class="search-icon"></i>
                <input type="text" placeholder="Parin goat?" class="search-input">
            </div>
            <div class="nav-links">
                <a href="/" class="nav-item">
                    <div class="icon-wrapper"><i data-lucide="home" width="22" height="22"></i></div>
                    <span class="nav-label">Home</span>
                </a>
                <a href="/jobs" class="nav-item active">
                    <div class="icon-wrapper"><i data-lucide="briefcase" width="22" height="22"></i></div>
                    <span class="nav-label">Jobs</span>
                </a>
                <a href="/explore-musicians" class="nav-item">
                    <div class="icon-wrapper"><i data-lucide="users" width="22" height="22"></i></div>
                    <span class="nav-label">Network</span>
                </a>
                <a href="/msg" class="nav-item">
                    <div class="icon-wrapper"><i data-lucide="message-square" width="22" height="22"></i></div>
                    <span class="nav-label">Messaging</span>
                </a>
                <a href="/requests" class="nav-item">
                    <div class="icon-wrapper"><i data-lucide="bell" width="22" height="22"></i></div>
                    <span class="nav-label">Notifications</span>
                </a>
                <% if (locals.user && locals.user.profilePic) { %>
                    <a href="/user/<%= user._id %>" class="user-avatar" style="background-image: url('<%= user.profilePic %>'); background-size: cover; background-position: center;"></a>
                <% } else { %>
                    <div class="user-avatar"></div>
                <% } %>
            </div>
        </nav>
    </div>
    <script>lucide.createIcons();</script>

    <div class="container" style="padding: 2rem 1rem;">
        <div class="header">
            <h1>My Job Postings</h1>
            <a href="/post-job" class="post-job-btn">+ Post New Job</a>
        </div>

        <% if (jobs && jobs.length > 0) { %>
            <div class="jobs-grid">
                <% jobs.forEach((job) => { %>
                    <div class="job-card">
                        <span class="status-badge status-<%= job.status %>"><%= job.status.toUpperCase() %></span>
                        <% if (job.selectedMusician) { %>
                            <div style="background-color: rgba(76, 175, 80, 0.2); padding: 0.5rem; border-radius: 8px; margin-bottom: 0.75rem; border: 1px solid #4caf50;">
                                <p style="color: #4caf50; font-size: 0.85rem; font-weight: 600; margin: 0;">
                                    ✓ Selected: <%= job.selectedMusician.username %>
                                </p>
                            </div>
                        <% } %>
                        <h3><%= job.title %></h3>
                        <p class="meta"><%= job.location %> • <%= new Date(job.createdAt).toLocaleDateString() %></p>
                        <p class="description"><%= job.description %></p>
                        <p class="payment">Payment: <%= job.payment %></p>
                        <div class="job-actions">
                            <a href="/applications/<%= job._id %>" class="view-applications-btn">View Applications</a>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <div class="empty-state">
                <h2>No jobs posted yet</h2>
                <p>Start by posting your first job opening!</p>
                <a href="/post-job" class="post-job-btn" style="margin-top: 1.5rem;">Post Your First Job</a>
            </div>
        <% } %>
    </div>
</body>
</html>

