<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Discover Gigs</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        :root {
            --bg-color: #050505;
            --card-bg: #111111;
            --card-hover: #151515;
            --nav-bg: #121212;
            --accent: #ffdec1;
            --accent-hover: #ffeadd;
            --text-main: #ffffff;
            --text-muted: #9ca3af;
            --border-color: rgba(255, 255, 255, 0.05);
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-color);
            color: var(--text-main);
            min-height: 100vh;
            overflow-y: scroll; /* Always show scrollbar to prevent layout shift */
            /* Standard scrollbar for Firefox */
            scrollbar-width: thin;
            scrollbar-color: #333 #0a0a0a;
        }

        /* Webkit Scrollbar */
        ::-webkit-scrollbar {
            width: 8px;
        }

        ::-webkit-scrollbar-track {
            background: #0a0a0a;
        }

        ::-webkit-scrollbar-thumb {
            background: #333;
            border-radius: 4px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #444;
        }

        /* Navbar Styles */
        .navbar-container {
            position: sticky;
            top: 0;
            z-index: 100;
            display: flex;
            justify-content: center;
            padding: 1.5rem 1rem 1rem 1rem;
            background-color: rgba(5, 5, 5, 0.95);
            backdrop-filter: blur(8px);
            -webkit-backdrop-filter: blur(8px);
        }

        .navbar {
            background-color: var(--nav-bg);
            border-radius: 9999px;
            padding: 0.75rem 1.5rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
            width: 100%;
            max-width: 64rem;
            border: 1px solid var(--border-color);
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.3), 0 4px 6px -2px rgba(0, 0, 0, 0.1);
        }

        .search-container {
            position: relative;
        }

        .search-icon {
            position: absolute;
            left: 0.75rem;
            top: 50%;
            transform: translateY(-50%);
            color: var(--text-muted);
            pointer-events: none;
            width: 18px;
            height: 18px;
        }

        .search-input {
            background-color: #1e1e1e;
            color: #d1d5db;
            font-size: 0.875rem;
            border-radius: 9999px;
            padding: 0.625rem 1rem 0.625rem 2.5rem;
            width: 16rem;
            border: none;
            outline: none;
            transition: all 0.2s;
        }

        .search-input:focus {
            box-shadow: 0 0 0 1px #4b5563;
            background-color: #252525;
        }

        .nav-links {
            display: flex;
            align-items: center;
            gap: 2rem;
        }

        .nav-item {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 0.25rem;
            cursor: pointer;
            color: var(--text-muted);
            transition: color 0.2s;
            text-decoration: none;
        }

        .nav-item:hover {
            color: #e5e7eb;
        }

        .nav-item.active {
            color: var(--accent);
        }

        .nav-item .icon-wrapper {
            transition: transform 0.2s;
        }

        .nav-item:hover .icon-wrapper {
            transform: scale(1.1);
        }

        .nav-label {
            font-size: 10px;
            font-weight: 500;
            letter-spacing: 0.025em;
        }

        .user-avatar {
            width: 2.5rem;
            height: 2.5rem;
            border-radius: 50%;
            background: linear-gradient(135deg, #9ca3af, #4b5563);
            border: 2px solid #121212;
            cursor: pointer;
            transition: opacity 0.2s;
        }

        .user-avatar:hover {
            opacity: 0.8;
        }

        /* Main Content */
        .main-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 2rem 1.5rem;
            padding-bottom: 5rem;
        }

        .header {
            margin-bottom: 2rem;
        }

        .header h1 {
            font-size: 2.25rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
            letter-spacing: -0.025em;
        }

        .header p {
            color: #6b7280;
            font-size: 0.875rem;
        }

        /* Filter Bar */
        .filter-bar {
            display: flex;
            flex-wrap: wrap;
            align-items: center;
            gap: 1.5rem;
            margin-bottom: 2rem;
            padding: 0 0.5rem;
        }

        .filter-group {
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .filter-label {
            color: var(--text-muted);
            font-size: 1.125rem;
        }

        .filter-btn {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.5rem 1.25rem;
            border-radius: 9999px;
            font-size: 0.875rem;
            font-weight: 500;
            border: none;
            cursor: pointer;
            transition: all 0.2s;
        }

        .filter-btn.active {
            background-color: var(--accent);
            color: black;
        }

        .filter-btn.active:hover {
            background-color: var(--accent-hover);
        }

        .filter-btn.inactive {
            background-color: #1e1e1e;
            color: white;
        }

        .filter-btn.inactive:hover {
            background-color: #2a2a2a;
        }

        /* Grid */
        .grid {
            display: grid;
            grid-template-columns: repeat(1, 1fr);
            gap: 1.5rem;
        }

        @media (min-width: 640px) {
            .grid { grid-template-columns: repeat(2, 1fr); }
        }
        @media (min-width: 1024px) {
            .grid { grid-template-columns: repeat(3, 1fr); }
        }
        @media (min-width: 1280px) {
            .grid { grid-template-columns: repeat(4, 1fr); }
        }

        /* Card */
        .card {
            background-color: var(--card-bg);
            border-radius: 2rem;
            padding: 1.5rem;
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
            border: 1px solid var(--border-color);
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
            transition: all 0.3s;
        }

        .card:hover {
            background-color: var(--card-hover);
            transform: translateY(-4px);
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.3);
            border-color: rgba(255, 255, 255, 0.1);
        }

        .card-avatar-container {
            position: relative;
            margin-bottom: 1rem;
        }

        .card-avatar-wrapper {
            width: 6rem;
            height: 6rem;
            border-radius: 50%;
            padding: 2px;
            background: linear-gradient(to top right, var(--accent), #a855f7);
        }

        .card-avatar {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            object-fit: cover;
            border: 4px solid var(--card-bg);
            display: block;
        }

        .card-title {
            font-size: 1.25rem;
            font-weight: 600;
            margin-bottom: 0.25rem;
            color: white;
        }

        .card-location {
            font-size: 0.75rem;
            color: #9ca3af;
            margin-bottom: 1rem;
        }

        .card-description {
            font-size: 0.875rem;
            color: #6b7280;
            margin-bottom: 1.5rem;
            line-height: 1.625;
            max-width: 90%;
        }

        .card-rating {
            display: flex;
            align-items: center;
            gap: 0.375rem;
            margin-bottom: 2rem;
        }

        .star-icon {
            color: var(--accent);
            fill: var(--accent);
            width: 14px;
            height: 14px;
        }

        .rating-value {
            font-size: 0.875rem;
            color: #d1d5db;
            font-weight: 500;
        }

        .rating-count {
            font-size: 0.875rem;
            color: #6b7280;
        }

        .card-actions {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 0.75rem;
            width: 100%;
            margin-top: auto;
        }

        .btn-primary {
            background-color: var(--accent);
            color: black;
            padding: 0.75rem 1rem;
            border-radius: 9999px;
            font-size: 0.875rem;
            font-weight: 600;
            border: none;
            cursor: pointer;
            transition: background-color 0.2s;
        }

        .btn-primary:hover {
            background-color: var(--accent-hover);
        }

        .btn-secondary {
            background-color: transparent;
            border: 1px solid #374151;
            color: #d1d5db;
            padding: 0.75rem 1rem;
            border-radius: 9999px;
            font-size: 0.875rem;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.2s;
        }

        .btn-secondary:hover {
            border-color: #6b7280;
            color: white;
        }
    </style>
</head>
<body>
    <div class="navbar-container">
        <nav class="navbar">
            <div class="search-container">
                <i data-lucide="search" class="search-icon"></i>
                <input type="text" placeholder="Parin goat?" class="search-input">
            </div>

            <div class="nav-links">
                <a href="/" class="nav-item">
                    <div class="icon-wrapper"><i data-lucide="home" width="22" height="22"></i></div>
                    <span class="nav-label">Home</span>
                </a>
                <a href="/jobs" class="nav-item active">
                    <div class="icon-wrapper"><i data-lucide="briefcase" width="22" height="22"></i></div>
                    <span class="nav-label">Jobs</span>
                </a>
                <a href="/explore-musicians" class="nav-item">
                    <div class="icon-wrapper"><i data-lucide="users" width="22" height="22"></i></div>
                    <span class="nav-label">Network</span>
                </a>
                <a href="/msg" class="nav-item">
                    <div class="icon-wrapper"><i data-lucide="message-square" width="22" height="22"></i></div>
                    <span class="nav-label">Messaging</span>
                </a>
                <a href="/requests" class="nav-item">
                    <div class="icon-wrapper"><i data-lucide="bell" width="22" height="22"></i></div>
                    <span class="nav-label">Notifications</span>
                </a>
                <% if (locals.user && locals.user.profilePic) { %>
                    <a href="/user/<%= user._id %>" class="user-avatar" style="background-image: url('<%= user.profilePic %>'); background-size: cover; background-position: center;"></a>
                <% } else { %>
                    <div class="user-avatar"></div>
                <% } %>
            </div>
        </nav>
    </div>

    <main class="main-container">
        <div class="header">
            <h1>Discover Gigs</h1>
            <p>work with talented musicians</p>
        </div>

        <div class="filter-bar">
            <div class="filter-group">
                <span class="filter-label">Genre:</span>
                <button class="filter-btn inactive">
                    All Genres
                    <i data-lucide="chevron-down" width="16" height="16"></i>
                </button>
            </div>
            <div class="filter-group">
                <span class="filter-label">Instrument:</span>
                <button class="filter-btn active">
                    Guitar
                    <i data-lucide="chevron-down" width="16" height="16"></i>
                </button>
            </div>
            <div class="filter-group">
                <span class="filter-label">Location:</span>
                <button class="filter-btn active">
                    Anywhere
                    <i data-lucide="chevron-down" width="16" height="16"></i>
                </button>
            </div>
        </div>

        <div class="grid">
            <% if (jobs && jobs.length > 0) { %>
                <% jobs.forEach((job, index) => { %>
                    <div class="card">
                        <div class="card-avatar-container">
                            <div class="card-avatar-wrapper">
                                <img src="https://picsum.photos/200/200?random=<%= index %>" alt="<%= job.title %>" class="card-avatar">
                            </div>
                        </div>
                        <h3 class="card-title"><%= job.title %></h3>
                        <p class="card-location"><%= job.hirer ? job.hirer.username : 'Hirer' %> • <%= job.location %></p>
                        <p class="card-description"><%= job.description.length > 100 ? job.description.substring(0, 100) + '...' : job.description %></p>
                        <div class="card-rating">
                            <i data-lucide="dollar-sign" class="star-icon"></i>
                            <span class="rating-value"><%= job.payment %></span>
                        </div>
                        <div class="card-actions">
                            <% if (user) { %>
                                <a href="/form/<%= job._id %>" class="btn-primary" style="text-decoration: none; display: block; text-align: center;">Apply Now</a>
                            <% } else { %>
                                <a href="/login" class="btn-primary" style="text-decoration: none; display: block; text-align: center;">Apply Now</a>
                            <% } %>
                            <a href="/jobs/<%= job._id %>" class="btn-secondary" style="text-decoration: none; display: block; text-align: center;">More info</a>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <div style="grid-column: 1 / -1; text-align: center; padding: 3rem; color: #666;">
                    <p>No jobs available at the moment. Check back later!</p>
                </div>
            <% } %>
        </div>
    </main>

    <script>
        lucide.createIcons();
    </script>
</body>
</html>