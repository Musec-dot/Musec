<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Create Account - Name</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet" />
  <link
    rel="stylesheet"
    href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
    crossorigin="anonymous"
    referrerpolicy="no-referrer"
  />
  <style>
    body {
    margin: 0;
    font-family: 'Montserrat', sans-serif;
    background-color: #000;
    color: #ffffff;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    overflow: hidden;
    position: relative;
}

.gradient-overlay-create-account-pass {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: radial-gradient(circle at bottom, rgba(100, 0, 255, 0.4) 0%, transparent 60%);
    opacity: 0.8;
    z-index: -1;
}

.create-account-pass-container {
    text-align: left;
    width: 90%;
    max-width: 400px;
    box-sizing: border-box;
    position: relative;
    z-index: 1;
}

.create-account-pass-container h1 {
    font-size: 2.2em;
    margin-bottom: 50px;
    font-weight: 600;
    letter-spacing: 1px;
    text-align: center;
}

.password-input-group {
    position: relative;
    margin-bottom: 20px;
    text-align: left;
}

.password-input-group input {
    width: 100%;
    padding: 10px 0;
    background: none;
    border: none;
    border-bottom: 2px solid rgba(255, 255, 255, 0.3);
    color: #ffffff;
    font-size: 1.1em;
    outline: none;
    transition: border-color 0.3s ease;
}

.password-input-group input::placeholder {
    color: rgba(255, 255, 255, 0.6);
}

.password-input-group input:focus {
    border-color: #6a11cb;
}

.password-input-group .underline {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0;
    height: 2px;
    background-color: #6a11cb;
    transition: width 0.3s ease;
}

.password-input-group input:focus + .underline {
    width: 100%;
}

.skip-text {
    font-size: 0.9em;
    color: #8A8A8A;
    margin-top: 20px;
    margin-bottom: 20px;
    margin-left: 120px;
    text-align: left;
}

.skip-link {
    color: #8A8A8A;
    text-decoration: none;
    font-weight: 500;
    transition: color 0.3s ease;
}

.next-button-pass {
    display: inline-flex;
    justify-content: center;
    align-items: center;
    width: auto;
    height: auto;
    background: none;
    border: none;
    border-radius: 0;
    text-decoration: none;
    color: rgba(255, 255, 255, 0.7);
    font-size: 1.8em;
    box-shadow: none;
    transition: transform 0.3s ease, color 0.3s ease;
    margin-top: 30px;
    margin-bottom: 50px;
    float: right;
    padding: 0;
}

.next-button-pass:hover {
    transform: translateY(-3px);
    color: #ffffff;
}

@media (max-width: 600px) {
    .create-account-pass-container {
        padding: 0 20px;
        width: 95%;
    }

    .create-account-pass-container h1 {
        font-size: 1.8em;
        margin-bottom: 40px;
    }

    .password-input-group input {
        font-size: 1em;
    }

    .next-button-pass {
        width: auto;
        height: auto;
        font-size: 1.5em;
        margin-top: 20px;
        margin-bottom: 40px;
    }
}
  </style>
</head>
<body class="create-account-pass-page">
  <div class="gradient-overlay-create-account-pass"></div>

  <div class="create-account-pass-container">
    <h1>Enter your Name</h1>

    <div class="password-input-group">
      <input id="username" placeholder="John Doe" required />
      <div class="underline"></div>
    </div>

    <p class="skip-text">
      <a href="/dashboard" class="skip-link">Click Here To Skip</a>
    </p>

    <button class="next-button-pass" onclick="submitSignup()">
      <i class="fas fa-arrow-right"></i>
    </button>
  </div>

  <script>
    function submitSignup() {
      const username = document.getElementById("username").value.trim();
      const email = sessionStorage.getItem("email");
      const password = sessionStorage.getItem("password");

      if (!username || !email || !password) {
        alert("Missing information. Please go back and fill out all fields.");
        return;
      }

      fetch("/signup", {
        method: "POST",
        headers: {
          "Content-Type": "application/x-www-form-urlencoded"
        },
        body: new URLSearchParams({ username, email, password })
      })
        .then((res) => {
          if (res.redirected) {
            window.location.href = res.url;
          } else {
            return res.text().then((text) => alert("Signup failed: " + text));
          }
        })
        .catch((err) => {
          console.error(err);
          alert("An error occurred. Please try again.");
        });
    }
  </script>
</body>
</html>
